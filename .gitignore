__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/
*.spec
version_info.txt
.venv/
venv/
.coverage
htmlcov/

# User sample files must never be committed. Binary samples of real formats
# (save games, project files, exports) routinely embed usernames, machine names
# and account identifiers, and a corpus large enough to analyse is large enough
# to leak them.
samples/
